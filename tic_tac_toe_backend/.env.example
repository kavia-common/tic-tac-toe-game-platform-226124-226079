# Environment variables for Tic Tac Toe Backend (FastAPI)

# PUBLIC INTERFACE: CORS configuration
# Comma-separated list of allowed origins for the frontend.
# Default is http://localhost:3000
# Set to "*" to allow all origins (not recommended for production).
CORS_ORIGINS=http://localhost:3000

# PUBLIC INTERFACE: Database configuration
# SQLite database file path. If not set, defaults to ./data/tictactoe.db
# Note: DB_PATH is also accepted for backward compatibility but TICTACTOE_DB_PATH is preferred.
TICTACTOE_DB_PATH=./data/tictactoe.db
# DB_PATH=./data/tictactoe.db
